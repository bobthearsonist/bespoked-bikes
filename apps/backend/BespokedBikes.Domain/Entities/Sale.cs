namespace BespokedBikes.Domain.Entities;

using BespokedBikes.Domain.Enums;

public class Sale
{
    // Id, CustomerId, SoldByEmployeeId, FulfilledByEmployeeId (nullable), ProductId, Status (SaleStatus enum), SalePrice, CommissionAmount, SaleChannel, Location (enum), SaleDate, FulfilledDate (nullable), CreatedAt, UpdatedAt
}
