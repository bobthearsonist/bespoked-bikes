namespace BespokedBikes.Domain.Entities;

public class Product
{
    // Id, ProductType, Name, Description, Supplier (string, will become supplier_id FK in future), CostPrice, RetailPrice, CommissionPercentage, CreatedAt, UpdatedAt
}
